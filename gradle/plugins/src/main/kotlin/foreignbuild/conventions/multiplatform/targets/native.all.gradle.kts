package foreignbuild.conventions.multiplatform.targets

plugins {
    id("foreignbuild.conventions.multiplatform.targets.native.desktop")
    id("foreignbuild.conventions.multiplatform.targets.native.ios")
}
